import React from 'react';
import PropTypes from 'prop-types';
import styles from './LaboratoryHome.module.css';

const LaboratoryHome = () => (
  <div className={styles.LaboratoryHome}>
    LaboratoryHome Component
  </div>
);

LaboratoryHome.propTypes = {};

LaboratoryHome.defaultProps = {};

export default LaboratoryHome;
